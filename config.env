# Enhanced Document Organization System Configuration
# Clean and simple configuration for cloud sync and document organization

# ============================================================================
# PATH CONFIGURATION
# ============================================================================

# Local sync hub - central location for all documents
SYNC_HUB="${HOME}/Sync_Hub_New"

# iCloud sync path (Obsidian documents)
ICLOUD_PATH="${HOME}/Library/Mobile Documents/iCloud~md~obsidian/Documents/Sync"

# Google Drive sync path (adjust the wildcard part to match your setup)
GOOGLE_DRIVE_PATH="${HOME}/Library/CloudStorage/GoogleDrive-moatasim23android@gmail.com/My Drive/Sync"

# Legacy paths (if still needed)
LEGACY_ICLOUD_PATH="${HOME}/Downloads/Data_Science/Sync_iCloud"
LEGACY_GOOGLE_DRIVE_PATH="${HOME}/Downloads/Data_Science/Sync_GoogleDrive"

# ============================================================================
# SYNC CONFIGURATION
# ============================================================================

# Sync behavior
SYNC_ENABLED=true
SYNC_BIDIRECTIONAL=true
SYNC_CONFLICT_RESOLUTION="newer"  # Options: newer, ask, backup

# Sync intervals (in seconds)
SYNC_INTERVAL=3600           # 1 hour
QUICK_SYNC_INTERVAL=300      # 5 minutes for important changes

# Retry configuration
MAX_SYNC_RETRIES=3
SYNC_RETRY_DELAY=300         # 5 minutes

# ============================================================================
# ORGANIZATION CONFIGURATION
# ============================================================================

# Enable/disable organization features
ORGANIZATION_ENABLED=true
AUTO_CATEGORIZATION=true
SIMPLIFIED_CATEGORIES=true   # Use 5 main categories instead of 47+

# Organization behavior
DRY_RUN_MODE=false          # Set to true to preview changes without applying
BACKUP_BEFORE_MOVE=true     # Create backups before moving files
PRESERVE_EXISTING_ORGANIZATION=true

# Content analysis settings
MIN_FILE_SIZE=10            # Minimum file size in bytes to process
MAX_FILENAME_LENGTH=80      # Warn about long filenames
CONTENT_ANALYSIS_DEPTH=50   # Lines to analyze for categorization

# ============================================================================
# SIMPLIFIED CATEGORIES (5 main categories)
# ============================================================================

# Category definitions for simplified mode
CATEGORY_AI_ML="🤖 AI & ML"
CATEGORY_RESEARCH="📚 Research Papers"  
CATEGORY_WEB="🌐 Web Content"
CATEGORY_NOTES="📝 Notes & Drafts"
CATEGORY_DEV="💻 Development"

# ============================================================================
# MCP SERVER CONFIGURATION
# ============================================================================

# MCP server settings for AI integration
MCP_ENABLED=true
MCP_PORT=3000
MCP_HOST="localhost"
MCP_AUTO_START=true

# ============================================================================
# LOGGING AND MONITORING
# ============================================================================

# Logging configuration
LOG_LEVEL="INFO"            # Options: DEBUG, INFO, WARN, ERROR
LOG_RETENTION_DAYS=7        # How long to keep log files
LOG_TO_FILE=true
LOG_TO_CONSOLE=true

# ============================================================================
# ADVANCED SETTINGS
# ============================================================================

# Performance settings
PARALLEL_PROCESSING=false   # Disable for stability
INCREMENTAL_PROCESSING=true # Only process new/changed files
BATCH_SIZE=50              # Files to process in one batch

# Safety settings
ENABLE_BACKUPS=true
BACKUP_RETENTION_DAYS=30
SAFE_MODE=true             # Extra confirmation for destructive operations

# Export all variables for use in scripts
export SYNC_HUB ICLOUD_PATH GOOGLE_DRIVE_PATH LEGACY_ICLOUD_PATH LEGACY_GOOGLE_DRIVE_PATH
export SYNC_ENABLED SYNC_BIDIRECTIONAL SYNC_CONFLICT_RESOLUTION
export SYNC_INTERVAL QUICK_SYNC_INTERVAL MAX_SYNC_RETRIES SYNC_RETRY_DELAY
export ORGANIZATION_ENABLED AUTO_CATEGORIZATION SIMPLIFIED_CATEGORIES
export DRY_RUN_MODE BACKUP_BEFORE_MOVE PRESERVE_EXISTING_ORGANIZATION
export MIN_FILE_SIZE MAX_FILENAME_LENGTH CONTENT_ANALYSIS_DEPTH
export CATEGORY_AI_ML CATEGORY_RESEARCH CATEGORY_WEB CATEGORY_NOTES CATEGORY_DEV
export MCP_ENABLED MCP_PORT MCP_HOST MCP_AUTO_START
export LOG_LEVEL LOG_RETENTION_DAYS LOG_TO_FILE LOG_TO_CONSOLE
export PARALLEL_PROCESSING INCREMENTAL_PROCESSING BATCH_SIZE
export ENABLE_BACKUPS BACKUP_RETENTION_DAYS SAFE_MODE